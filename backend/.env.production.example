# Production Environment Variables
# COPY THIS to .env in production and fill in real values

# SECURITY: Generate a new SECRET_KEY for production!
# Run: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-production-secret-key-generate-a-new-one

# Always False in production!
DEBUG=False

# Your production domains (comma-separated, no spaces)
ALLOWED_HOSTS=.onrender.com,.railway.app,.herokuapp.com,yourdomain.com

# Frontend URLs for CORS (comma-separated, no spaces, include https://)
CORS_ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://yourdomain.com

# Production Database URL (PostgreSQL)
# Render/Railway/Heroku will provide this automatically
DATABASE_URL=postgresql://user:password@host:5432/database
